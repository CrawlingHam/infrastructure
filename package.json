{
    "scripts": {
        "infra:apply": "cd src && tofu apply",
        "infra:plan": "cd src && tofu plan",
        "infra:init": "cd src && tofu init",
        "scripts": "node scripts/index.js"
    },
    "devDependencies": {
        "dotenv": "^16.4.5"
    }
}
