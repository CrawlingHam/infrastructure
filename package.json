{
    "scripts": {
        "init:plan": "cd src/terraform && tofu init && tofu plan",
        "apply": "cd src/terraform && tofu init && tofu apply",
        "plan": "cd src/terraform && tofu plan"
    },
    "devDependencies": {
        "dotenv": "^16.4.5"
    }
}
